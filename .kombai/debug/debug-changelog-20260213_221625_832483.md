## Changes Made

### 1. Removed icon overlay from category cards
- **File:** `park-babelsberg/assets/parks-renderer.js`
- **Change:** Removed the `.category-card-icon` div from `CategoryCard.render()` method (lines 115-128). Both the image-present and no-image branches no longer emit the icon element.
- **Revert:** Restore the `<div class="category-card-icon">${this.data.icon}</div>` inside the `.category-card-image` div in both branches.

### 2. Improved category card text alignment & readability
- **File:** `park-babelsberg/assets/style.css`
- **Changes:**
  - Softened the image overlay gradient (from `rgba(0,0,0,0.4)` to `rgba(0,0,0,0.15)`)
  - Replaced `.category-card-icon` CSS with improved `.category-card-content` layout (flexbox column with gap, better padding)
  - Updated `.category-card-content h3` with consistent line-height, proper flex gap, wrapping
  - Updated `.category-count` with `var(--radius-full)` and `var(--text-muted)` tokens
  - Updated `.category-card-content p` with `var(--text-secondary)`, letter-spacing, refined line-height
  - Updated mobile breakpoint to adjust image height instead of icon size
- **Revert:** Restore original `.category-card-icon`, `.category-card-content`, `.category-count`, `.category-card-content p` CSS rules.

### 3. Swapped pillar hero image
- **File:** `park-babelsberg/index.html`
- **Change:** Changed `<img src="images/park-babelsberg/jagdschloss.jpeg">` to `<img src="images/park-babelsberg/schloss_allee.jpeg">` in the `.pillar-page .hero` section (line 235). Updated alt text to "Schlossallee im Park Babelsberg".
- **Revert:** Change src back to `images/park-babelsberg/jagdschloss.jpeg` and alt to `"Parks in Potsdam"`.

## Revert Status
- [ ] Change 1 - Remove category card icons
- [ ] Change 2 - Text alignment improvements
- [ ] Change 3 - Hero image swap
